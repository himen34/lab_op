#include "CDoubleLinkedList.h"
#include "BasicFunctions.h"
int main() {
	string listtype;
	cout << "Enter the type specifier to build the double list:";
	cin >> listtype;
	ProgRunner(listtype);
}